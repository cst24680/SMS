function StudyBackupActions({ fileName, setFileName, onWrite, onRead, onAppend, onRename, message }) {
  return <section className="rounded-3xl border border-amber-200 bg-amber-50 p-5 shadow-sm xl:col-span-2"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-amber-700">Exercise 7 file actions</p><p className="mt-1 text-sm text-slate-600">Backup your MongoDB activity to a server-side text file.</p></div><div className="flex flex-wrap gap-2"><button type="button" onClick={onWrite} className="btn btn-primary">Write export</button><button type="button" onClick={onRead} className="btn btn-secondary">Read file</button><button type="button" onClick={onAppend} className="btn btn-secondary">Append latest</button></div></div><div className="mt-4 flex flex-wrap gap-2"><input value={fileName} onChange={(event) => setFileName(event.target.value)} className="input-field max-w-xs" aria-label="Backup filename" /><button type="button" onClick={onRename} className="btn btn-secondary">Rename file</button></div>{message ? <p className="mt-3 text-sm text-slate-600">{message}</p> : null}</section>;
}

export default StudyBackupActions;
